#include<stdio.h>
int main() {
    int mark = 80;
    if(mark >= 40)
    {
        printf("the mark of %d is pass\n", mark);
    }
    else
    {
        printf("the mark of %d is false\n", mark);
    }
    return 0;
}