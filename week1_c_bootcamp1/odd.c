#include<stdio.h>
    int main(){

        int a;
        // int x = 10; //x is the first x odd numbers
        // int odd = x * 2;

        // for(a = 1; a < odd; a++)
        // {
        //    printf("the odd numbers are")
        // }
        for(a = 0; a < 10; a++) 
        {
            printf("%d\n", a*2+1);// the maths formula are useful in c 
        }
    return 0;
}